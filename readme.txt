1. Dependence
Before installing and running the algorithm, make sure to install the following dependencies.
1)Ubuntu(We run the algorithm in ununtu18.04.)
2)Opencv
3)ROS(melodic)


2.Installation and run
1)Use the terminal to open the downloaded file.Then enter the following commands in turn.
2)catkin_make
3)cd devel
4)source setup.bash
5)rosrun forest demo synthia
